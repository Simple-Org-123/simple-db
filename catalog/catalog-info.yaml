apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: simple-db
  annotations:
    "backstage.io/kubernetes-label-selector": "app.kubernetes.io/part-of=simple-db"
spec:
  type: service
  lifecycle: experimental
  owner: default-team
  providesApis:
    - simple-db
